[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "self-supervised-learning-methods"
version = "0.1.0"
description = "A comprehensive framework for self-supervised learning methods including contrastive learning, predictive modeling, and representation learning"
authors = [
    {name = "kryptologyst", email = "kryptologyst@example.com"}
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Image Recognition",
]

dependencies = [
    # Core ML/DL
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "torchaudio>=2.0.0",
    "numpy>=1.24.0",
    "pandas>=2.0.0",
    "scikit-learn>=1.3.0",
    "scipy>=1.10.0",
    
    # Computer Vision
    "opencv-python>=4.8.0",
    "kornia>=0.7.0",
    "albumentations>=1.3.0",
    
    # Data handling
    "tqdm>=4.65.0",
    "pyyaml>=6.0",
    "omegaconf>=2.3.0",
    "h5py>=3.9.0",
    
    # Visualization
    "matplotlib>=3.7.0",
    "seaborn>=0.12.0",
    "plotly>=5.15.0",
    "tensorboard>=2.13.0",
    
    # Evaluation & Metrics
    "faiss-cpu>=1.7.4",
    "hnswlib>=0.7.0",
    
    # Self-supervised learning specific
    "transformers>=4.30.0",
    "timm>=0.9.0",
    
    # Development tools
    "black>=23.0.0",
    "ruff>=0.0.280",
    "mypy>=1.5.0",
    "pytest>=7.4.0",
    "pre-commit>=3.3.0",
]

[project.optional-dependencies]
dev = [
    "black>=23.0.0",
    "ruff>=0.0.280", 
    "mypy>=1.5.0",
    "pytest>=7.4.0",
    "pre-commit>=3.3.0",
    "jupyter>=1.0.0",
    "ipykernel>=6.25.0",
]

demo = [
    "streamlit>=1.25.0",
    "gradio>=3.40.0",
]

tracking = [
    "wandb>=0.15.0",
    "mlflow>=2.5.0",
]

serving = [
    "fastapi>=0.100.0",
    "uvicorn>=0.23.0",
]

[project.urls]
Homepage = "https://github.com/kryptologyst/self-supervised-learning-methods"
Repository = "https://github.com/kryptologyst/self-supervised-learning-methods"
Documentation = "https://github.com/kryptologyst/self-supervised-learning-methods#readme"
"Bug Tracker" = "https://github.com/kryptologyst/self-supervised-learning-methods/issues"

[project.scripts]
ssl-train = "src.train:main"
ssl-eval = "src.eval:main"
ssl-demo = "src.demo:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.black]
line-length = 88
target-version = ['py310']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
'''

[tool.ruff]
target-version = "py310"
line-length = 88
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[[tool.mypy.overrides]]
module = [
    "torch.*",
    "torchvision.*",
    "cv2.*",
    "kornia.*",
    "albumentations.*",
    "transformers.*",
    "timm.*",
    "faiss.*",
    "hnswlib.*",
    "matplotlib.*",
    "seaborn.*",
    "plotly.*",
    "tensorboard.*",
    "wandb.*",
    "mlflow.*",
    "streamlit.*",
    "gradio.*",
    "fastapi.*",
    "uvicorn.*",
]
ignore_missing_imports = true
